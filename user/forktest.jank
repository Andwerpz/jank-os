#include <jstring>;
#include <string>;
#include <iostream>;

i32 main() {

    cout << "RUNNING FORKTEST\n";

    for(i32 i = 0; i < 5; i++){
        sys_fork();
    }

    i64 pid = sys_getpid();
    string out = "PID : " + to_string(pid) + "\n";
    cout << out;

    return 0;
}
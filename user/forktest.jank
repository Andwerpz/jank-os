#include <jstring>;
#include <string>;
#include <iostream>;

i32 main() {

    for(i32 i = 0; i < 5; i++){
        sys_fork();
    }

    u64 pid = sys_getpid();
    string out = "PID : " + to_string(pid) + "\n";
    cout << out;

}
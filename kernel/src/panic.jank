
void panic(u8* msg) {
    enable_vga_print();
    print("panic : ");
    print(msg);
    print("\n");
    while(1) {
        asm!("hlt");
    }
}

//'panic assert'
void assert(i32 val, u8* msg) {
    if(!val) panic(msg);
}

void assert(u64 val, u8* msg) {
    if(!val) panic(msg);
}
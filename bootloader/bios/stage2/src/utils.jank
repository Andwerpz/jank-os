
void print(u8 c) {
    vga_print(c);
} 

void print(u8* str) {
    vga_print(str);
}

void print(u64 x) {
    vga_print(x);
}

void print(u32 x){
    vga_print(x);
}

void print(u16 x) {
    vga_print(x);
}

void print(i64 x) {
    vga_print("i64");
}

void print(i32 x) {
    vga_print("i32");
}

template<T>
T max(T x, T y) {
    if(x > y) return x;
    return y;
}

template<T>
T min(T x, T y) {
    if(x < y) return x;
    return y;
}

#include <jstring>;
#include <string>;
#include <iostream>;

i32 main() {

    cout << "before fork\n";

    sys_fork();
    sys_fork();

    for(i32 i = 0; i < 5; i++) {
        
    }

    pid_t pid = sys_getpid();
    string out = "PID : " + to_string(pid) + "\n";
    cout << out;

    while(1) {}

    return 0;
}
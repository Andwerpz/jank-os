#include <jstring>;
#include <string>;
#include <iostream>;

i32 main() {

    cout << "before fork\n";

    for(i32 i = 0; i < 5; i++) {
        sys_fork();
    }

    pid_t pid = sys_getpid();
    string out = "PID : " + to_string(pid) + "\n";
    cout << out;

    return 0;
}